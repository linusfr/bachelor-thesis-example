apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-postgres-persistentvolumeclaim
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
